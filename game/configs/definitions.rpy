# #sound
# init -2 python:
#     renpy.music.register_channel("sfx", "sound", False)
#     renpy.music.register_channel("music2", "music", loop=True)

#constants
define develop_mode = True

default persistent.first_start = True
default persistent.font_type = "default"

define splash_enabled = False